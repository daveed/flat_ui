require 'rubygems'
require 'rake'
require 'jeweler'

Jeweler::Tasks.new do |gem|
  gem.name = "flat_ui"
  gem.homepage = "http://github.com/reorient/flat_ui"
  gem.license = "MIT"
  gem.summary = "FlatUi wraps and installs the free version of Flat UI into a rails asset pipeline."
  gem.description = "FlatUi wraps and installs the free version of Flat UI into a rails asset pipeline."
  gem.email = "dalphen@reorientgroup.com"
  gem.authors = ["dalphen"]
end
Jeweler::RubygemsDotOrgTasks.new